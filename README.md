## Cinema Ticket Booking – Frontend Portfolio

این ریپازیتوری یک **رابط کاربری نمونه برای رزرو آنلاین بلیط سینما** است که به عنوان پروژه‌ی پورتفولیو طراحی شده و هنوز به‌صورت کامل به یک محصول عملیاتی متصل به بک‌اند تبدیل نشده است.

### 🎯 هدف پروژه
- **نمایش مهارت در طراحی رابط کاربری (UI)** با تمرکز روی دیزاین مدرن، تم تیره و فضای سینمایی.  
- **پیاده‌سازی تجربه کاربری (UX) مناسب** برای رزرو بلیط، مرور فیلم‌ها و انتخاب سینما.  
- **شناخت ساختار یک وب‌اپ رزرو بلیط** به صورت فرانت‌اند استاتیک که در آینده قابل توسعه به نسخه‌ی واقعی است.

### 🚀 قابلیت‌های فعلی
- **صفحه اصلی رزرو بلیط سینما** با هدر، منوی موبایل و سکشن‌های مختلف معرفی سرویس.  
- **لیست فیلم‌ها** (مثل «هم اکنون در سینما» و «۱۰ فیلم برتر این هفته») بر پایه‌ی داده‌های ماک استاتیک.  
- **جستجوی فیلم** در هدر و نسخه‌ی موبایل با نمایش نتایج در همان صفحه.  
- **کامپوننت‌های مختلف رابط کاربری** مثل:  
  - کارت فیلم (`movieCard`)  
  - هیرو فیلم (`movieHero`)  
  - توضیحات فیلم (`movieDescription`)  
  - امتیاز فیلم (`movieScore`)  
  - کارت بازیگر (`actorCard`)  
  - آمار کلی (`statistics`)  
  - اسلاید/تب صفحات فیلم (`tabs-page-movie`)  
- **صفحات نمونه‌ی چندمرحله‌ای رزرو** مثل انتخاب سینما و انتخاب صندلی در مسیر رزرو (به صورت رابط کاربری استاتیک).  
- **بخش سوالات متداول (FAQ)** با باز و بسته شدن پاسخ‌ها.  
- **فرم عضویت خبرنامه** در انتهای صفحه.  
- **طراحی راست‌به‌چپ (RTL)** و فارسی‌سازی کامل رابط کاربری.  
- **ریسپانسیو نسبی** برای نمایش روی دسکتاپ و برخی اندازه‌های موبایل (هنوز در حال بهبود است).  
- **استفاده از داده‌های ماک** برای فیلم‌ها، سینماها و سانس‌ها (فایل‌های `moviesData.js`, `cinemaData.js`, `showTimes.js`).  
- **بدون وابستگی به فریم‌ورک‌های جاوااسکریپتی** – فقط با HTML، CSS و Vanilla JS پیاده‌سازی شده است.

### 🧩 تکنولوژی‌ها و ساختار پروژه
- **HTML5** برای ساختار صفحات (`index.html` و سایر صفحات داخل پوشه‌ی `pages`).  
- **CSS3** با ساختار ماژولار:  
  - استایل‌های پایه در `styles/base` (Reset, رنگ‌ها، فونت، استایل‌های پیش‌فرض).  
  - استایل کامپوننت‌ها در `styles/components`.  
  - استایل صفحات مختلف در `styles/pages`.  
- **JavaScript (ES Modules)** در پوشه‌ی `src` برای:  
  - مدیریت کامپوننت‌های UI  
  - منطق ساده‌ی فیلتر، جستجو و رندر کارت‌ها  
  - بارگذاری و استفاده از داده‌های ماک  
- **بدون وابستگی بک‌اند** – همه‌چیز در سمت فرانت‌اند و در مرورگر اجرا می‌شود.  
- **انتشار روی GitHub Pages** به عنوان یک پروژه‌ی پورتفولیو فرانت‌اند.

### 📁 راه‌اندازی و اجرا
1. این ریپازیتوری را کلون یا دانلود کنید.  
2. پروژه را با یک **Live Server** (مثلاً افزونه‌ی VS Code یا هر سرور استاتیک ساده) باز کنید تا مسیرهای ماژول‌های ES در مرورگر به‌درستی کار کنند.  
3. فایل `index.html` را در مرورگر باز کنید و رابط کاربری را مشاهده کنید.  

> توجه: چون این پروژه فقط فرانت‌اند است، نیازی به نصب پکیج‌های خاص یا اجرای سرور بک‌اند نیست.

### 🔭 برنامه‌های توسعه‌ی آینده
این پروژه در حال حاضر **یک نمونه‌ی فرانت‌اند برای نمایش مهارت‌ها** است، اما در ادامه برنامه‌های زیر برای توسعه‌ در نظر گرفته شده است:

- **اتصال به بک‌اند واقعی / API** برای دریافت لیست فیلم‌ها، سانس‌ها و سینماها.  
- **پیاده‌سازی سیستم احراز هویت** (ثبت‌نام، ورود، بازیابی رمز عبور).  
- **انتخاب صندلی به صورت داینامیک**.  
- **بهبود کامل ریسپانسیو** برای همه‌ی سایزهای موبایل و تبلت.  
- **اضافه کردن انیمیشن‌ها و ترنزیشن‌های نرم‌تر** برای بهبود تجربه‌ی کاربری.  
- **بهینه‌سازی ساختار کد جاوااسکریپت** (تفکیک بهتر ماژول‌ها و مدیریت وضعیت).  

### 📌 وضعیت فعلی
- این پروژه در حال حاضر **به عنوان یک Frontend Portfolio** منتشر شده است.  
- داده‌ها همگی **ساختگی (Mock Data)** هستند و عملیات رزرو، لاگین، پرداخت و… صرفاً در حد رابط کاربری نمایش داده می‌شوند.  
- این ریپازیتوری در آینده **به‌روزرسانی و گسترش** خواهد شد تا به یک سیستم رزرو بلیط قابل استفاده‌ی واقعی نزدیک‌تر شود.

